<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dine In</title>
    <style>
      @import url("https://fonts.googleapis.com/css?family=Montserrat:400,800");
      /* Source: Florin Pop https://codepen.io/FlorinPop17/pen/vPKWjd */

      .navigation_bar {
        /* background-color: #ffffff; */
        margin: -8px -8px 0px -8px;
        display: flex;
        flex-direction: row;
        /* align-content: center; */
        align-items: center;

        justify-content: space-between;
      }

      .mobile-nav-toggle {
        display: none;
      }

      ul {
        font-family: "Montserrat", sans-serif;
        /* width: 100%; */
        list-style-type: none;
        margin: 0;
        padding: 0;
        overflow: hidden;
        text-decoration: none;
        font-family: "Montserrat", sans-serif;
        font-size: 18px;
        font-weight: bold;
        letter-spacing: 1px;
        padding-right: 10px;

        display: flex;
        /* margin-inline-end: 0.75em; */
      }
      li {
        float: right;
      }
      li a {
        display: block;
        color: #26ab7b;
        text-align: center;
        padding: 14px 16px;
        text-decoration: none;
      }
      li a:hover {
        background-color: #111;
        color: #f1f1f1;
      }
      li a,
      .dropbtn {
        display: inline-block;
        text-align: center;
        padding: 14px 16px;
        text-decoration: none;
      }

      li a:hover,
      .dropdown:hover .dropbtn {
        background-color: #f9f9f9;
        color: #26ab7b;
      }

      li.dropdown {
        display: inline-block;
      }

      .dropdown-content {
        display: none;
        position: absolute;
        background-color: #f9f9f9;
        min-width: 160px;
        box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.2);
        z-index: 1;
      }

      .dropdown-content a {
        color: #26ab7b;
        padding: 12px 16px;
        text-decoration: none;
        display: block;
        text-align: left;
      }

      .dropdown-content a:hover {
        background-color: #f1f1f1;
      }

      .dropdown:hover .dropdown-content {
        display: block;
      }

      @media (max-width: 35em) {
        ul {
          display: flex;
          position: fixed;
          z-index: 1000;
          inset: 0 0 0 30%;
          flex-direction: column;
          padding: min(20vh, 8rem) 1em;
          background: hsl(0 0% 0% / 0.8);
          /* background-color: #f6f5f7;  instead of blur*/
          transform: translateX(100%);
          transition: transform 350ms ease-out;
        }

        @supports (backdrop-filter: blur(1rem)) {
          ul {
            background: hsl(0 0% 100% / 0.1);
            backdrop-filter: blur(3rem);
          }
        }

        ul[data-visible="true"] {
          transform: translateX(0%);
        }

        .mobile-nav-toggle {
          display: block;
          position: fixed;
          top: 2rem;
          right: 2rem;
          z-index: 9999;
          background-color: transparent;
          background-image: url("/images/hamburger-menu.png");
          background-repeat: no-repeat;
          background-size: 50px;
          padding: 0;
          margin: 0;
          width: 50px;
          aspect-ratio: 1;
          
          border: 0;
        }

        .mobile-nav-toggle[data-visible="true"] {
          background-image: url("images/menu-close.png");
        }
      }
    </style>
  </head>
  <body>
    <div class="navigation_bar">
      <a class="logo_img" href="/"
        ><img
          src="/images/dineInLogoTransparent.png"
          alt="Dine In Logo"
          width="120px"
          height="120px"
      /></a>

      <button class="mobile-nav-toggle" data-visible="false"></button>

      <ul class="link-list" data-visible="false">
        <!-- <li class="dropdown">
          <a href="/recipes" class="dropbtn">Our Recipes</a>
          <div class="dropdown-content">
            <a href="/recipes">Breakfast</a>
            <a href="#">Lunch</a>
            <a href="#">Dinner</a>
          </div>
        </li> -->
        <li><a class="Recipes_Link" href="/recipes"> Our Recipes </a></li>
        <li><a class="Blog_Link" href="/main"> Blog </a></li>
        <li><a class="Information_Link" href="/info"> Team </a></li>
      </ul>
    </div>

    <script>
      const linkList = document.querySelector(".link-list");
      const navToggle = document.querySelector(".mobile-nav-toggle");

      navToggle.addEventListener("click", () => {
        const visibility = linkList.getAttribute("data-visible");

        if (visibility === "false") {
          linkList.setAttribute("data-visible", true);
          navToggle.setAttribute("data-visible", true);
        } else {
          linkList.setAttribute("data-visible", false);
          navToggle.setAttribute("data-visible", false);
        }
      });
    </script>
  </body>
</html>
